/**
 * 山熏 · 演示版全局配置（公开仓库）
 * 仅本地 JSON + localStorage，不含生产 API 连接
 */
window.SX_CONFIG = {
  pageSize: 50,
  maxPageSize: 200,
  dataVersion: '1',
  demoData: {
    batches: 'data/demo/batches.json',
  },
};
